"use client"

import { ArrowRight, Github, Linkedin, Mail, ExternalLink, Code2, Terminal, Database, Cpu } from "lucide-react"
import Link from "next/link"

export default function Portfolio() {
  return (
    <div className="min-h-screen bg-background text-foreground selection:bg-primary selection:text-primary-foreground">
      {/* Navigation */}
      <nav className="fixed top-0 w-full z-50 bg-background/80 backdrop-blur-md border-b border-border/40">
        <div className="max-w-5xl mx-auto px-6 h-16 flex items-center justify-between">
          <span className="font-bold text-lg tracking-tight">Eduardo Vegas</span>
          <div className="flex gap-6 text-sm font-medium text-muted-foreground">
            <Link href="#work" className="hover:text-foreground transition-colors">Proyectos</Link>
            <Link href="#experience" className="hover:text-foreground transition-colors">Experiencia</Link>
            <Link href="#contact" className="hover:text-foreground transition-colors">Contacto</Link>
          </div>
        </div>
      </nav>

      {/* Hero Section */}
      <section className="pt-32 pb-20 px-6">
        <div className="max-w-5xl mx-auto">
          <div className="max-w-3xl space-y-8">
            <div className="inline-flex items-center rounded-full border border-border bg-secondary/50 px-3 py-1 text-sm text-secondary-foreground">
              <span className="flex h-2 w-2 rounded-full bg-green-500 mr-2 animate-pulse"></span>
              Disponible para nuevos proyectos
            </div>

            <h1 className="text-5xl md:text-7xl font-bold tracking-tight text-foreground">
              Desarrollador Full Stack especializado en <span className="text-muted-foreground">soluciones empresariales.</span>
            </h1>

            <p className="text-xl text-muted-foreground leading-relaxed max-w-2xl">
              Transformo requerimientos complejos en software robusto. Especializado en CRM, automatización con IA y optimización de rendimiento web.
            </p>

            <div className="flex flex-wrap gap-4 pt-4">
              <Link
                href="#work"
                className="inline-flex h-12 items-center justify-center rounded-md bg-primary px-8 text-sm font-medium text-primary-foreground shadow transition-colors hover:bg-primary/90 focus-visible:outline-none focus-visible:ring-1 focus-visible:ring-ring"
              >
                Ver Proyectos
                <ArrowRight className="ml-2 h-4 w-4" />
              </Link>
              <Link
                href="mailto:emzovg@gmail.com"
                className="inline-flex h-12 items-center justify-center rounded-md border border-input bg-background px-8 text-sm font-medium shadow-sm transition-colors hover:bg-accent hover:text-accent-foreground focus-visible:outline-none focus-visible:ring-1 focus-visible:ring-ring"
              >
                Contactar
              </Link>
            </div>
          </div>
        </div>
      </section>

      {/* Tech Stack - Minimalist */}
      <section className="py-12 border-y border-border/40 bg-card/30">
        <div className="max-w-5xl mx-auto px-6">
          <p className="text-sm font-medium text-muted-foreground mb-8 uppercase tracking-wider">Stack Tecnológico</p>
          <div className="grid grid-cols-2 md:grid-cols-4 lg:grid-cols-6 gap-8 opacity-70 grayscale hover:grayscale-0 transition-all duration-500">
            {/* Simple text/icon representation for a cleaner look */}
            {["React", "Next.js", "TypeScript", "Laravel", "PostgreSQL", "AWS"].map((tech) => (
              <div key={tech} className="flex items-center gap-2 font-semibold text-lg">
                <Terminal className="h-5 w-5" /> {tech}
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Experience Section */}
      <section id="experience" className="py-24 px-6">
        <div className="max-w-5xl mx-auto">
          <h2 className="text-3xl font-bold mb-12 tracking-tight">Experiencia Profesional</h2>
          <div className="space-y-12">
            {[
              {
                role: "Asistente de Programación en Marketing Digital",
                company: "Interamericana Norte SAC",
                period: "Feb 2025 – Presente",
                description: "Liderando la implementación de ecosistemas CRM con IA.",
                achievements: [
                  "Reducción del 40% en tiempo de respuesta mediante Chatbot IA.",
                  "Automatización de procesos ahorrando +30 horas/mes al equipo comercial.",
                  "Optimización de funnel de ventas incrementando leads en 20%."
                ]
              },
              {
                role: "Practicante de Marketing Digital",
                company: "Interamericana Norte SAC",
                period: "Oct 2024 – Ene 2025",
                description: "Optimización de rendimiento web y análisis de datos.",
                achievements: [
                  "Mejora de Core Web Vitals (Lighthouse 61 -> 90).",
                  "Reducción de costos operativos en un 40% mediante in-housing.",
                  "Integración de datos en tiempo real con APIs de Ford."
                ]
              }
            ].map((job, i) => (
              <div key={i} className="group relative border-l-2 border-border pl-8 hover:border-primary transition-colors duration-300">
                <div className="absolute -left-[9px] top-0 h-4 w-4 rounded-full border-2 border-background bg-muted-foreground group-hover:bg-primary transition-colors duration-300" />
                <div className="flex flex-col sm:flex-row sm:items-center sm:justify-between mb-2">
                  <h3 className="text-xl font-semibold text-foreground">{job.role}</h3>
                  <span className="text-sm font-medium text-muted-foreground bg-secondary/50 px-3 py-1 rounded-full">{job.period}</span>
                </div>
                <p className="text-lg text-muted-foreground mb-4">{job.company}</p>
                <ul className="space-y-2">
                  {job.achievements.map((item, j) => (
                    <li key={j} className="text-muted-foreground flex items-start">
                      <span className="mr-2 mt-1.5 h-1.5 w-1.5 rounded-full bg-primary/50 shrink-0" />
                      {item}
                    </li>
                  ))}
                </ul>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Projects Section */}
      <section id="work" className="py-24 px-6 bg-secondary/20">
        <div className="max-w-5xl mx-auto">
          <h2 className="text-3xl font-bold mb-12 tracking-tight">Proyectos Destacados</h2>
          <div className="grid md:grid-cols-2 gap-8">
            {[
              {
                title: "CRM INSAC",
                desc: "Sistema comercial integral con IA para gestión de oportunidades.",
                tags: ["Laravel", "PHP", "Groq AI", "MySQL"],
                stats: "40% más eficiencia",
                link: "#"
              },
              {
                title: "Catálogo Digital Interactivo",
                desc: "Plataforma de visualización de vehículos con panel administrativo.",
                tags: ["Laravel", "JavaScript", "Tailwind"],
                stats: "+20 leads mensuales",
                link: "#"
              }
            ].map((project, i) => (
              <div key={i} className="group rounded-lg border border-border bg-card p-6 hover:border-primary/50 transition-all duration-300 hover:shadow-lg">
                <div className="flex justify-between items-start mb-4">
                  <div className="p-3 rounded-md bg-primary/10 text-primary">
                    <Database className="h-6 w-6" />
                  </div>
                  <Link href={project.link} className="text-muted-foreground hover:text-primary transition-colors">
                    <ExternalLink className="h-5 w-5" />
                  </Link>
                </div>
                <h3 className="text-xl font-bold mb-2 group-hover:text-primary transition-colors">{project.title}</h3>
                <p className="text-muted-foreground mb-6">{project.desc}</p>
                <div className="flex flex-wrap gap-2 mb-6">
                  {project.tags.map(tag => (
                    <span key={tag} className="px-2 py-1 text-xs font-medium rounded-md bg-secondary text-secondary-foreground">
                      {tag}
                    </span>
                  ))}
                </div>
                <div className="pt-4 border-t border-border flex items-center text-sm font-medium text-primary">
                  <Cpu className="h-4 w-4 mr-2" />
                  {project.stats}
                </div>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Skills Grid */}
      <section className="py-24 px-6">
        <div className="max-w-5xl mx-auto">
          <h2 className="text-3xl font-bold mb-12 tracking-tight">Habilidades Técnicas</h2>
          <div className="grid grid-cols-2 md:grid-cols-4 gap-4">
            {[
              { name: "JavaScript", level: "Experto" },
              { name: "PHP / Laravel", level: "Experto" },
              { name: "React / Next.js", level: "Avanzado" },
              { name: "SQL / PostgreSQL", level: "Avanzado" },
              { name: "Python / FastAPI", level: "Intermedio" },
              { name: "Git / CI/CD", level: "Avanzado" },
              { name: "Docker", level: "Intermedio" },
              { name: "AWS", level: "Básico" },
            ].map((skill, i) => (
              <div key={i} className="p-4 rounded-md border border-border bg-card hover:bg-secondary/50 transition-colors">
                <div className="font-semibold">{skill.name}</div>
                <div className="text-xs text-muted-foreground mt-1">{skill.level}</div>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Contact Section */}
      <section id="contact" className="py-24 px-6 border-t border-border">
        <div className="max-w-3xl mx-auto text-center">
          <h2 className="text-4xl font-bold mb-6 tracking-tight">¿Listo para empezar?</h2>
          <p className="text-xl text-muted-foreground mb-10">
            Actualmente estoy disponible para proyectos freelance y consultoría.
            Si tienes un proyecto en mente, hablemos.
          </p>
          <div className="flex justify-center gap-6">
            <Link
              href="mailto:emzovg@gmail.com"
              className="flex items-center gap-2 text-lg font-medium hover:text-primary transition-colors"
            >
              <Mail className="h-5 w-5" />
              emzovg@gmail.com
            </Link>
            <Link
              href="https://linkedin.com/in/eduardo-vegas"
              className="flex items-center gap-2 text-lg font-medium hover:text-primary transition-colors"
            >
              <Linkedin className="h-5 w-5" />
              LinkedIn
            </Link>
            <Link
              href="https://github.com"
              className="flex items-center gap-2 text-lg font-medium hover:text-primary transition-colors"
            >
              <Github className="h-5 w-5" />
              GitHub
            </Link>
          </div>
        </div>
      </section>

      {/* Footer */}
      <footer className="py-8 text-center text-sm text-muted-foreground border-t border-border/40">
        <p>© {new Date().getFullYear()} Eduardo Vegas. Construido con Next.js & Tailwind.</p>
      </footer>
    </div>
  )
}
